class 안에 클래스가 있으면 이너클래스가 됨.

클래스 안에 선언 가능한 요소
 - 변수, 생성자, 메서드
 
static 타입 변수명; // 클래스 변수명
타입 변수명; // 인스턴스 변수
=> 처음부터 끝까지 사용할 수 있는 변수
=> 모든 객체가 똑같은 식을 사용할 때

클래스명(){} // 기본 생성자
클래스명(인스턴스 변수에 맞게 지역변수 선언){
	this.인스턴스변수 = 지역변수; ...
}

super : 조상

super() : super 클래스의 생성자 호출
		  모든 인스턴스 멤버는 객체 생성시 생성자를 거쳐야 하는데
		  super 클래스의 멤버는 super 생성자를 거쳐야 함
		  
super. : super 참조변수. super클래스의 멤버를 가리킴.